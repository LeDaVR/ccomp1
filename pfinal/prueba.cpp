/*#include "terreno.h"
#include <iostream>
using namespace std;

int main(){
	Terreno terreno(80);
    for(int i=0;i<terreno.indices.getSize();i++)
    	cout<<terreno.indices.arr[i]<<" ";
    cout<<endl;
    cout<<terreno.vertices.getSize()<<endl;
    
    for(int i=0;i<terreno.vertices.getSize();i++){
    	if(!(i%3)&&i>0)
    		cout<<endl;
    	cout<<terreno.vertices.arr[i]<<" ";
    	
	}
}*/
